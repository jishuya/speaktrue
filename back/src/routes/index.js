const authRoutes = require('./auth');
const chatRoutes = require('./chat');
const nvcRoutes = require('./nvc');
const historyRoutes = require('./history');

module.exports = {
  authRoutes,
  chatRoutes,
  nvcRoutes,
  historyRoutes,
};
